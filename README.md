# Local-Mind

Minimal React + Vite UI for sending requests to a running ComfyUI HTTP API and exporting responses as Obsidian-friendly artifacts.

## Run locally

```bash
npm install
npm run dev
```

Default API base URL: `http://localhost:8188` (configurable via `VITE_COMFYUI_API` or in the UI).